# Things to add/fix


## Channels

* `sub_to_fsaverage()` outputs a matrix, should output a dict to show which rows correspond to which electrode
* `pial_to_inflated()` outputs a matrix, should output a dict to show which rows correspond to which electrode

## IEEG2NWB


## FileIO


